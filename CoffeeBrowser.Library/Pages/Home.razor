@page "/"
@* @using Microsoft.AspNetCore.Authorization;
@attribute [Authorize] *@
<h1>Hello, world!</h1>

<AuthorizeView>
    <Authorized>
        Welcome to Coffees Browser app.
    </Authorized>
    <NotAuthorized>
        Please log in.
    </NotAuthorized>
</AuthorizeView>

@* Now to you want to show some data, only admin can access
    **Work with Roles**
    After that you can write below code than go to CustomAuthStateProvider class
    there you can add --ClaimTypes.Role-- (For data access )
 *@
<AuthorizeView>
    <div style="background-color: #444; color:white; padding:10px">
        This is visible only admins
    </div>
</AuthorizeView>

